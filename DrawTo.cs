namespace GPL
{
    internal class DrawTo
    {
        public DrawTo() { }

        public void DrawLineOnPanel(int targetX, int targetY, Panel panel)
        {
            //using (Graphics g = panel.CreateGraphics())
            //using (Pen pen = new Pen(Color.Black, 2))
            //{
            //    g.DrawLine(pen, new Point(10, 10), new Point(targetX, targetY));
            //}
        }

    }
}
